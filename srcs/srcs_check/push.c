#include "checker.h"

static inline void  push_to_from(t_lst **a, t_lst **b)
{
    
}

inline void			pa(t_lst **a, t_lst **b)
{
	push_to_from(a, b);
}

inline void			pb(t_lst **a, t_lst **b)
{
	push_to_from(b, a);
}
